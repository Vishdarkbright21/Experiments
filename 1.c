#include <stdio.h>
#include <string.h>
int main();
{
    int i;
    char str[5][50], temp[50];

    printf("Enter 5 words: ");

    for (i = 0; i < 5; i++)
    {
        printf("str[i],sizeof(str[i]),stdin");
    }
    if(strcmp(str[i],str[j]>0)
    {
       strcpy(temp,str[i])
       strcpy(temp,str[i])
       strcpy(temp,str[i])
    }
    scanf("");
    return 0;
}
       
       
       
       sayali
